[project]
name = "fde-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "anthropic>=0.125.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.1",
    "rich>=15.0.0",
    "typer>=0.23.2",
]

[project.scripts]
fde-cli = "fde_cli:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
